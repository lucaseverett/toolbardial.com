{
  "name": "toolbardial.com",
  "scripts": {
    "start": "parcel ./src/index.html --open --no-cache",
    "build": "rimraf dist && parcel build ./src/index.html --no-cache"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2",
    "sass": "^1.26.5"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
